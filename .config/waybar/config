[
  {
    "layer": "top", // Waybar on highest layer so tooltips go over windows
    "output": "eDP-1", // Set output to primary monitor
    "height": 36, // Set height to avoid jumping due to active workspace indicator

    "modules-left": ["hyprland/workspaces"], // Sets modules for the left of the bar
    "modules-center": ["hyprland/window", "clock#date", "clock#time"], // Set modules for the center of the bar
    "modules-right": ["network", "battery", "temperature", "wireplumber", "tray"], // Set modules for the right of the bar

    "include": ["$XDG_CONFIG_HOME/waybar/modules.jsonc"] // Include the module config
  }
]
